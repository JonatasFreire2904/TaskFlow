@using Microsoft.AspNetCore.Components.Routing
@using Microsoft.AspNetCore.Components.Web
@using TaskFlow.Blazor.Layout
@using TaskFlow.Blazor.Services

<Router AppAssembly="@typeof(App).Assembly">
    <Found Context="routeData">
        @if (IsLoginPage(routeData.PageType))
        {
            <RouteView RouteData="@routeData" Layout="@typeof(LoginLayout)" />
        }
        else
        {
            <RouteView RouteData="@routeData" DefaultLayout="@typeof(MainLayout)" />
        }
        <FocusOnNavigate RouteData="@routeData" Selector="h1" />
    </Found>
    <NotFound>
        <PageTitle>Not found</PageTitle>
        <LayoutView Layout="@typeof(MainLayout)">
            <p role="alert">Sorry, there's nothing at this address.</p>
        </LayoutView>
    </NotFound>
</Router>

@code {
    private bool IsLoginPage(Type pageType)
    {
        return pageType?.Name == "Login";
    }
}
